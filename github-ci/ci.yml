name: "Execute CI/CD Scan"
on:
  pull_request:
    types: [opened, synchronize, closed]
  push:
    branches:
      - main

jobs:
  scan:
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.2

      - name: Set up Node.js
        uses: actions/setup-node@v4.1.0
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm install

      - name: Run GitHub Action
        id: scan
        # uses: DSDO01/ci-cd-playground/actions/github@main
        uses: ./.github/actions
        with:
          API_TOKEN: dedge_pat_bnvbdZ_l39qk1Gnt1seD4hdf-bmvBRXCXrIuRwb3pUk=
          DEDGE_HOST_URL: https://scanning.free.beeceptor.com
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ASSET_ID: 047f620c-3e47-4142-a153-47d572295fa2
